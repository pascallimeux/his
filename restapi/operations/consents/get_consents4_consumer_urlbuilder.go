package consents

// This file was generated by the swagger tool.
// Editing this file might prove futile when you re-run the generate command

import (
	"errors"
	"net/url"
	golangswaggerpaths "path"
	"strings"
)

// GetConsents4ConsumerURL generates an URL for the get consents4 consumer operation
type GetConsents4ConsumerURL struct {
	Appid      string
	Consumerid string

	_basePath string
	// avoid unkeyed usage
	_ struct{}
}

// WithBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *GetConsents4ConsumerURL) WithBasePath(bp string) *GetConsents4ConsumerURL {
	o.SetBasePath(bp)
	return o
}

// SetBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *GetConsents4ConsumerURL) SetBasePath(bp string) {
	o._basePath = bp
}

// Build a url path and query string
func (o *GetConsents4ConsumerURL) Build() (*url.URL, error) {
	var result url.URL

	var _path = "/his/v0/api/app/{appid}/consumer/{consumerid}/consents"

	appid := o.Appid
	if appid != "" {
		_path = strings.Replace(_path, "{appid}", appid, -1)
	} else {
		return nil, errors.New("Appid is required on GetConsents4ConsumerURL")
	}
	consumerid := o.Consumerid
	if consumerid != "" {
		_path = strings.Replace(_path, "{consumerid}", consumerid, -1)
	} else {
		return nil, errors.New("Consumerid is required on GetConsents4ConsumerURL")
	}
	_basePath := o._basePath
	result.Path = golangswaggerpaths.Join(_basePath, _path)

	return &result, nil
}

// Must is a helper function to panic when the url builder returns an error
func (o *GetConsents4ConsumerURL) Must(u *url.URL, err error) *url.URL {
	if err != nil {
		panic(err)
	}
	if u == nil {
		panic("url can't be nil")
	}
	return u
}

// String returns the string representation of the path with query string
func (o *GetConsents4ConsumerURL) String() string {
	return o.Must(o.Build()).String()
}

// BuildFull builds a full url with scheme, host, path and query string
func (o *GetConsents4ConsumerURL) BuildFull(scheme, host string) (*url.URL, error) {
	if scheme == "" {
		return nil, errors.New("scheme is required for a full url on GetConsents4ConsumerURL")
	}
	if host == "" {
		return nil, errors.New("host is required for a full url on GetConsents4ConsumerURL")
	}

	base, err := o.Build()
	if err != nil {
		return nil, err
	}

	base.Scheme = scheme
	base.Host = host
	return base, nil
}

// StringFull returns the string representation of a complete url
func (o *GetConsents4ConsumerURL) StringFull(scheme, host string) string {
	return o.Must(o.BuildFull(scheme, host)).String()
}
